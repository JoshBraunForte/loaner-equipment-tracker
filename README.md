# SampleProject

This is a disposable repository used to kick the tires on Application Factory V1.

After bootstrap, `.factory/` contains the factory snapshot this project is using.
Project-specific artifacts belong under `docs/`.
